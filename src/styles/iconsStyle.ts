import React from "react";

// search component
const iconSearch = {
  fontSize: "1.30rem",
  color: "#c1c1c8",
  position: "relative",
  left: "2rem",
  top: ".30rem",
} as React.CSSProperties;

const iconRemoveInputValue = {
  fontSize: "1.40rem",
  color: "#c1c1c8",
  position: "relative",
  right: "2rem",
  top: ".35rem",
  cursor: "pointer",
} as React.CSSProperties;

export { iconSearch, iconRemoveInputValue };
